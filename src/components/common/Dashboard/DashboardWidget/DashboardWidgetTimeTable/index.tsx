import React from "react";
import {DashboardWidgetTimeTableProps} from "./props";
import {Box} from "@mantine/core";
import classes from "./DashboardWidgetTimeTable.module.css";

export const DashboardWidgetTimeTable: React.FC<DashboardWidgetTimeTableProps> = (props: DashboardWidgetTimeTableProps) => {
    return (
        <Box className={classes.main_container}>

        </Box>
    );
};